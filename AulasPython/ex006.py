#Crie uma função 'repetir_texto(texto, vezes)' que imprimeo texto repetindo o número de vezes indicado.

texto = input('')
vezes = int(input('Números de vezes: '))

def repetir_texto(texto, vezes):
    repetir = texto * vezes
    return repetir ()
print(repetir_texto(texto, vezes))